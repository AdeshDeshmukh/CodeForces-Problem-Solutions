#include <bits/stdc++.h>
using namespace std;
using ll  = long long;
using vi  = vector<int>;
using vll = vector<ll>;
#define pb push_back
#define sorta(v) sort((v).begin(), (v).end())
#define foo(i, a, b) for (int i = (a); i < (b); ++i)
 
 
//---------------------ANSWER------------------------// 
 
 
 
 
void Adesh() {
    int n;cin >> n;
    vi pa(n+1);
    foo(i,0,n) {
        int dd;cin >> dd;
        pa[dd] = i;
    }
    vi b(n);
    foo(i, 0, n) {
        cin >> b[i];
    }
 
    if (n <= 1) {
        cout << 0 << endl ;
        return;
    }
 
    int ml = 1, cl = 1;
    foo(i, 1, n) {
        if (pa[b[i - 1]] < pa[b[i]]) cl++;
        else {
            cl = 1;
        }
        ml = max(ml, cl);
    }
 
    cout << n - ml << endl;
}
 
 
 
//---------------------ANSWER------------------------// 
 
 
 
 
 
int main() {
#ifndef ONLINE_JUDGE
    freopen("Error.txt", "w", stderr);
#endif
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
 
    
 //  ll t = 1 ;
    int t; cin >> t; 
    while (t--) { 
        Adesh();
    }
 
    return 0;
    
    
}
